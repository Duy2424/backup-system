# Thu vien cho he thong backup (ban da pin dung version theo bao cao - muc 3.2.1)
# Cai dat: pip install -r requirements.txt
#
# Flask        - web framework cho Storage Server (web UI + REST API)
# Werkzeug     - WSGI core cua Flask (di kem Flask, pin de tranh lech version)
# Requests     - agent goi REST API len server (HTTP client)
# cryptography - ma hoa AES-256 CBC + sinh khoa Fernet theo tung user
#
# (Jinja2, click, itsdangerous, blinker se duoc cai keo theo Flask.)

flask==3.0.0
werkzeug==3.0.1
requests==2.31.0
cryptography==42.0.5
